language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t insaf730hamzin/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true insaf730hamzin/docker-react npm run test